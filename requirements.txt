# Currency Exchange Data Pipeline — third-party dependencies
# (stdlib modules such as datetime, csv, io, and uuid are not included here)

# HTTP — fetches exchange rate APIs (open.er-api.com, Fed H.10 via GitHub)
requests==2.32.3

# PySpark — Bronze/Silver/Gold transformations
pyspark==3.5.1

# Delta Lake — DeltaTable, MERGE (SCD2 and incremental fact table)
delta-spark==3.2.0


streamlit
psycopg2-binary
pandas
groq
